<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Titre de la page</title>
</head>
<body>
   {% for user in users%}
    <li>{{user.firstname }} {{user.lastname}}</li>
    {% endfor %}
</body>
</html>